# rsyslog drop-in for Samba vfs_full_audit
# app/setup/samba.sh が ENABLE_AUDIT="yes" のときに /etc/rsyslog.d/40-samba-audit.conf として配置する。
#
# Samba (vfs_full_audit) は LOCAL5 ファシリティへ監査イベントを送出する。
# それを専用ファイルに振り分け、デフォルトの /var/log/syslog 等への重複出力を停止する。

local5.*    /var/log/samba/audit.log
& stop
